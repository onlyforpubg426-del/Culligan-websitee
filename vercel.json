{
  "buildCommand": "pnpm --filter @workspace/culligan-pk run build",
  "outputDirectory": "artifacts/culligan-pk/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "env": {
    "NODE_ENV": "production",
    "BASE_PATH": "/"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
