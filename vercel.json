{
  "version": 2,
  "projects": [
    {
      "src": "artifacts/culligan-pk",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/public",
        "buildCommand": "npm run build"
      }
    }
  ]
}
